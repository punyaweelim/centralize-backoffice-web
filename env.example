# .env.example
# คัดลอกไฟล์นี้เป็น .env และแก้ไขค่าตามความเหมาะสม

# Backend API URL
VITE_API_URL=https://your-backend-api.com

# ใช้ Vite Proxy หรือไม่ (แนะนำสำหรับ development กับ self-signed cert)
# ถ้าเป็น true จะใช้ proxy configuration จาก vite.config.ts
# ถ้าเป็น false จะเรียก API โดยตรง
VITE_USE_PROXY=true

# Environment
VITE_ENV=development
